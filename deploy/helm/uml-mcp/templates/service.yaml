apiVersion: v1
kind: Service
metadata:
  name: {{ include "uml-mcp.fullname" . }}
  labels:
    {{- include "uml-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uml-mcp.selectorLabels" . | nindent 4 }}

# Publish the container image to GHCR

- **Priority:** P1   **Size:** S
- **Blocked on:** maintainer decision on image name/tags and enabling `packages: write` for the release workflow.
- **Unblock:** approve `ghcr.io/antoinebou12/uml-mcp`; then Helm `image.repository` gets a default.
